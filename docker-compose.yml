version: '3.8'

services:
  buck-it-up:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: buck-it-up
    ports:
      - "8080:8080"
    environment:
      - BUCKITUP_DB_PATH=/app/data/data.db
      - BUCKITUP_DATA_PATH=/app/data
      - BUCKITUP_ADMIN_PASSWORD=admin
    volumes:
      - buck-data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - buck-network

volumes:
  buck-data:
    driver: local

networks:
  buck-network:
    driver: bridge

